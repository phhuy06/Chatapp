{
    "devCommand": "uvicorn main:app --port 3000",
    "builds": [
      {
        "src": "main.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "main.py"
      }
    ],
    "installCommand": "pip install -r requirements.txt"
  }